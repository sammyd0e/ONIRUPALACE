[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Pin Node version for Netlify builds (Netlify respects .nvmrc and this env var)
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
